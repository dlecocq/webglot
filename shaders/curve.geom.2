;
}

void main() {
	
	int i;
	
	i = 0;
	float x;
	float a;
	float b;
	float h;
	
	a = gl_PositionIn[0].x;
	b = gl_PositionIn[1].x;
	h = (b - a) / 10.0;
	
	for (i = 0; i <= 10; i++) {
		x = (i * h) + a;
